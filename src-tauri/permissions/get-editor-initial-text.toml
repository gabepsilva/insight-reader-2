# Permission to invoke get_editor_initial_text (used by the editor page on mount)
[[permission]]
identifier = "allow-get-editor-initial-text"
description = "Allows the editor window to read the initial text passed when opening"
commands.allow = ["get_editor_initial_text"]
